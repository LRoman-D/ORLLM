# Replace this with the timestamped preference file produced by:
# steporlm-local build-preferences --rollout-path ...
preference_path: runs/preferences_7b/preferences.jsonl
output_dir: runs/dpo_data_7b
run_root: runs
run_prefix: dpo_data
